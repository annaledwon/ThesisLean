% In this file you should put all LaTeX macros and settings to be used both by
% the pdf version and the web version.
% This should be most of your macros.

% The theorem-like environments defined below are those that appear by default
% in the dependency graph. See the README of leanblueprint if you need help to
% customize this.
% The configuration below use the theorem counter for all those environments
% (this is what the [theorem] arguments mean) and never resets it.
% If you want for instance to number them within chapters then you can add
% [chapter] at the end of the next line.

\usepackage{ marvosym }

\newtheorem{theorem}{Theorem}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}

\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}





%------------------------------------------------------------------------------------------
\newcommand*{\MP}[1]{\, \text{#1}}

\newcommand*{\RR}{\mathbb{R}}
\newcommand*{\set}[1]{\left\{ #1 \right\}}
\newcommand*{\abs}[1]{\left\vert #1 \right\vert}

\newcommand*{\eqv}{\Leftrightarrow}                     % equivalence arrow
\newcommand*{\imp}{\Rightarrow}                         % implication arrow
\newcommand*{\qimp}{\quad\imp\quad}                     % implication arrow with gaps on both sides
\newcommand*{\pmi}{\Leftarrow}                          % reverse implication arrow
\newcommand*{\impu}[2][\imp]{\underset{#2}{#1}}

\newcommand*{\segment}[1]{\overline{#1}}
\newcommand*{\ray}[1]{\overrightarrow{#1}}
\newcommand{\geomline}[1]{\overleftrightarrow{#1}}
\newcommand*{\geomtriang}[1]{\bigtriangleup{#1}}

\newcommand*{\coord}[1]{\operatorname{coord}({#1})}
\newcommand*{\distance}[1]{\operatorname{d}({#1})}
\newcommand*{\interior}[1]{\operatorname{Int}({#1})}
\newcommand*{\exterior}[1]{\operatorname{Ext}({#1})}
\newcommand*{\supp}[2]{{#1}\operatorname{supp}{#2}}
\newcommand*{\comp}[2]{{#1}\operatorname{comp}{#2}}
\newcommand*{\vertAng}[2]{{#1}\operatorname{vert}{#2}}
%
